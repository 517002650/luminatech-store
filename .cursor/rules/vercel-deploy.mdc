---
description: 功能提交后 push + Vercel 生产部署并验收
alwaysApply: true
---

# Vercel 部署约定

功能改动（路由、API、schema、middleware、支付、后台页面/UI 等）**必须上线才算完成**。仅改 `docs/`、`.cursor/rules/`、注释等不影响线上行为的，可只 push、不部署。

## 完整流程（push + 部署 + 验收）

在 `e:\项目\独立站\web` 依次执行：

```powershell
# 1. 确认已 commit，工作区干净
git status
git log -1 --oneline

# 2. 推送到 GitHub
git push origin main

# 3. 主动触发生产部署（不要假设 push 会自动部署）
npx vercel --prod --yes
```

**部署成功标志**（CLI 输出）：

- `Build Completed`
- `Aliased` → `https://517002650-luminatech-store.vercel.app`
- JSON 中 `"status": "ok"`、`"readyState": "READY"`、`"target": "production"`

**验收清单**：

| 检查 | 期望 |
|------|------|
| `https://517002650-luminatech-store.vercel.app/zh` | 200，前台正常 |
| `/admin` 或新后台路径 | 未登录 → **307 跳转登录**，不是 **404** |
| 本次改动的具体功能 | 登录后台/前台点验（如菜单排序、新页面、API） |
| [Deployments](https://vercel.com/dashan4/517002650-luminatech-store) | 最新一条 **Ready** 且挂到 Production |

验收失败：修代码 → 重新 commit → 重复上述三步，勿只停在本地。

## 备选与注意

- Dashboard：**Deployments → Redeploy**（改 env 后勾选 Clear Cache）
- 改环境变量后必须 Redeploy，否则线上仍用旧 env
- Hobby / Webhook / 作者校验都可能导致 `git push` 后线上仍是旧版

细节与踩坑见 `docs/DEPLOYMENT.md` §5.1 / §5.2。

## middleware 体积

Edge middleware **禁止** import 会拉进 Prisma / Node 重依赖的模块。Cookie 等常量放独立轻量文件（例：`src/lib/affiliate-cookie.ts`）。
