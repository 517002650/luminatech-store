---
description: 功能提交后触发 Vercel 生产重新部署并验收
alwaysApply: true
---

# Vercel 部署约定

功能改动（路由、API、schema、middleware、支付、后台页面等）**必须上线才算完成**：commit → `git push origin main` → **主动触发生产部署** → 验收。仅改 `docs/`、`.cursor/rules/`、注释等不影响线上行为的，可只 push、不部署。

## 如何触发重新部署

优先（本机已登录 Vercel CLI）：

```powershell
cd "e:\项目\独立站\web"
npx vercel --prod --yes
```

备选：Dashboard → Deployments → Redeploy；或改 env 后必须 Redeploy。  
细节与踩坑见 `docs/DEPLOYMENT.md` §5.1。

**不要假设** `git push` 会自动部署。Hobby / Webhook / 作者校验都可能导致线上仍是旧版。

## 验收

- 生产域名：`https://517002650-luminatech-store.vercel.app`
- 新页面未登录应为跳转登录（307），不应 404
- CLI 输出含 Production 别名且 `status: ok`；失败则修完再部署，勿只停在本地

## middleware 体积

Edge middleware **禁止** import 会拉进 Prisma / Node 重依赖的模块。Cookie 等常量放独立轻量文件（例：`src/lib/affiliate-cookie.ts`）。
